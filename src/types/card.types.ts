export interface ICard{
    id: string,
    cardTitle: string,
    cardUserFullName: string,
    cardNumber : string,
    cardDateMonth: number,
    cardDateYear: number,
    cardBalance: number,
    cardCVC: string,
    cardStatus: boolean,
    created_at:string
}